import {serverFetch} from "@/lib/server-api";export default async function Page(){const rows=await serverFetch("/api/finance/control-audit/");return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Finance control audit</h1><p className="mt-1 text-slate-600">Immutable record of controlled finance actions and decisions.</p></div><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Timestamp</th><th className="p-3">Action</th><th className="p-3">Actor</th><th className="p-3">Linked records</th><th className="p-3">Details</th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.id} className="border-t align-top"><td className="p-3">{new Date(r.created_at).toLocaleString()}</td><td className="p-3 font-medium">{r.action}</td><td className="p-3">{r.actor_username||"System"}</td><td className="p-3 text-xs">{r.wallet?`Wallet ${r.wallet} `:""}{r.action_request?`Request ${r.action_request} `:""}{r.settlement_batch?`Settlement ${r.settlement_batch}`:""}</td><td className="max-w-md p-3"><pre className="whitespace-pre-wrap text-xs">{JSON.stringify(r.metadata||{})}</pre></td></tr>):<tr><td colSpan={5} className="p-8 text-center text-slate-500">No finance audit events.</td></tr>}</tbody></table></div></div>}
