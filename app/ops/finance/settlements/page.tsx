import { serverFetch } from "@/lib/server-api";
import SettlementActions from "./SettlementActions";
const money=(v:string,c="NGN")=>new Intl.NumberFormat("en-NG",{style:"currency",currency:c}).format(Number(v||0));
export default async function SettlementsPage(){const batches=await serverFetch("/api/finance/settlements/");return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Settlements</h1><p className="mt-1 text-slate-600">Review, approve and record payment of beneficiary settlements.</p></div><div className="overflow-hidden rounded-2xl border bg-white shadow-sm"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Beneficiary</th><th className="p-3">Period</th><th className="p-3">Status</th><th className="p-3 text-right">Amount</th><th className="p-3">Reference</th><th className="p-3">Action</th></tr></thead><tbody>{batches.length?batches.map((b:any)=><tr key={b.id} className="border-t"><td className="p-3 font-medium">{b.beneficiary_organization_name}</td><td className="p-3">{b.period_start} – {b.period_end}</td><td className="p-3">{b.status}</td><td className="p-3 text-right font-semibold">{money(b.total_amount,b.currency)}</td><td className="p-3">{b.external_reference||"—"}</td><td className="p-3"><SettlementActions id={b.id} status={b.status}/></td></tr>):<tr><td colSpan={6} className="p-8 text-center text-slate-500">No settlement batches yet.</td></tr>}</tbody></table></div></div>}
