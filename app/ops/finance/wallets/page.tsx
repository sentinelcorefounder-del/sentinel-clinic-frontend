import { serverFetch } from "@/lib/server-api";
const money=(v:string,c="NGN")=>new Intl.NumberFormat("en-NG",{style:"currency",currency:c}).format(Number(v||0));
export default async function WalletsPage(){const wallets=await serverFetch("/api/finance/wallets/");return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Organisation Wallets</h1><p className="mt-1 text-slate-600">Available, reserved and spendable balances for hospitals and clinics.</p></div><div className="overflow-hidden rounded-2xl border bg-white shadow-sm"><table className="w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="p-3">Organisation</th><th className="p-3">Currency</th><th className="p-3 text-right">Available</th><th className="p-3 text-right">Reserved</th><th className="p-3 text-right">Credit limit</th><th className="p-3 text-right">Spendable</th><th className="p-3">Status</th></tr></thead><tbody>{wallets.length?wallets.map((w:any)=><tr key={w.id} className="border-t"><td className="p-3 font-medium">{w.organization_name}</td><td className="p-3">{w.currency}</td><td className="p-3 text-right">{money(w.available_balance,w.currency)}</td><td className="p-3 text-right">{money(w.reserved_balance,w.currency)}</td><td className="p-3 text-right">{money(w.credit_limit,w.currency)}</td><td className="p-3 text-right font-semibold">{money(w.spendable_balance,w.currency)}</td><td className="p-3">{w.is_active?"Active":"Inactive"}</td></tr>):<tr><td colSpan={7} className="p-8 text-center text-slate-500">No wallets created yet.</td></tr>}</tbody></table></div></div>}
